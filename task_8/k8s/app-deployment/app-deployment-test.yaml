---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-web-test
  namespace: myapp-test
  labels:
    app: myapp
    component: web
    environment: test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp
      component: web
      environment: test
  template:
    metadata:
      labels:
        app: myapp
        component: web
        environment: test
    spec:
      containers:
      - name: myapp-web
        image: myapp:latest  # Replace with your actual image
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8080
          name: http
        env:
        # Database connection details
        - name: DB_HOST
          value: "myapp-db-test.myapp-test.svc.cluster.local"
        - name: DB_PORT
          value: "5432"
        - name: DB_NAME
          valueFrom:
            secretKeyRef:
              name: myapp-db-secret
              key: database
        - name: DB_USERNAME
          valueFrom:
            secretKeyRef:
              name: myapp-db-secret
              key: username
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: myapp-db-secret
              key: password
        resources:
          limits:
            memory: "256Mi"
            cpu: "250m"
          requests:
            memory: "128Mi"
            cpu: "100m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
---
apiVersion: v1
kind: Service
metadata:
  name: myapp-web-service
  namespace: myapp-test
  labels:
    app: myapp
    component: web
    environment: test
spec:
  type: ClusterIP
  selector:
    app: myapp
    component: web
    environment: test
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
